node_modules/
.pnpm-store/
.next/
dist/
*.log
.DS_Store

# Local secrets. The engine reads these; they must never be committed.
.env
.env.local

# Local agent instructions. Keep machine- and assistant-specific guidance out
# of the public repository.
AGENTS.md
CLAUDE.md

# TypeScript incremental build state — machine-local, churns on every typecheck.
*.tsbuildinfo

# Native macOS and Rust build products.
apps/macos/.build/
apps/engine-rs/target/
