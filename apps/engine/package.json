{
  "name": "@todoagent/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "// dev": "Goes through dev.ts, which loads the repo-root .env before starting the server. `start` and the test suites use server.ts directly, so a real API key never reaches the suites — see dev.ts. Proxy handling is in src/proxy.ts, not here: it has to run inside the process to reach the undici instance pi uses.",
    "dev": "tsx watch src/dev.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test --experimental-strip-types 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.83.0",
    "@hono/node-server": "1.13.7",
    "@todoagent/core": "workspace:*",
    "hono": "4.6.20",
    "typebox": "1.3.7",
    "undici": "8.5.0",
    "zod": "3.25.76"
  }
}
